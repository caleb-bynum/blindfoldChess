class Parser:
    def __init__(self, fen)
        self.matrix = [[]]
        self.rowStrings = self.fen.split("/")
        self.rows = [[]]
        for rs in self.rowStrings:
            self.rows.append([c for c in rs]) 
def parse(fen):
    rowStrings = fen.split('/')
    rows = [] #double array with each entry being a single char from the FEN string
    
    for rs in rowStrings:
        rows.append([c for c in rs])
        
    def parseRow(self):

        

